export function createSyncMutateSocket() {
  // mirrors createSocket and creates sync and mutate sockets.
}

// Implements the components of the socket interface used by Zero
class CompositeSocket {}
